<!DOCTYPE html>
<html lang="en">

<head>
  <%- include("../partials/head"); %>
</head>

<body class="container">
  <header>
    <%- include("../partials/header",{title:album.name}); %>
  </header>
  
  <main>
    <div class="gallery">
      <% album.photos.forEach(photo => { %>
       <img src=<%= photo %> alt="">
      <% }) %>
    </div>
  </main>

</body>

</html>
